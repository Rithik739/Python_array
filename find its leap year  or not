def leap_year(year):
    if ((year %400 ==0) or
        (year %100!=0) and
        (year %4==0)):
            return "leap year"
    else:
        return "not a leap year"
        
year=int(input("enter year "))
a=leap_year(year)
print(a)
