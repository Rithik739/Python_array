def checkprime(n):
    if n>1:
        for i in range(2, int(n/2)+1):
            if n%i==0:
                return 'not prime'
        else:
            return 'prime'
    else:
        return 'not prime'
n=int(input('enter the number '))
a=checkprime(n)
print(a)
